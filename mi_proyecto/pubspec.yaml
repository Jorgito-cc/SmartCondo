name: my_proyecto
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.0.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  camera: ^0.11.2
  image_picker: ^1.2.0

  # QR: escanear y generar
  #qr_code_scanner: ^1.0.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.1.2

  # (opcional) íconos extra; no es necesario porque Material ya trae íconos
  # icons_flutter: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
